{
  "name": "time-management",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@11.1.3",
  "scripts": {
    "lint": "eslint .",
    "format": "prettier --check README.md package.json pnpm-workspace.yaml tsconfig.base.json tsconfig.json eslint.config.js prettier.config.cjs vitest.config.ts \"apps/**/package.json\" \"apps/**/tsconfig.json\" \"apps/**/*.test.ts\" \"packages/**/package.json\" \"packages/**/tsconfig.json\" \"packages/**/*.test.ts\" \"packages/auth/**/*.ts\" \"packages/db/**/*.ts\"",
    "format:write": "prettier --write .",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "git clean -fdX",
    "db:generate": "pnpm --filter @time-management/db db:generate",
    "db:check": "pnpm --filter @time-management/db db:check",
    "db:migrate": "pnpm --filter @time-management/db db:migrate",
    "db:seed": "pnpm --filter @time-management/db db:seed"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0",
    "typescript-eslint": "^8.0.0",
    "vitest": "^3.0.0"
  }
}
